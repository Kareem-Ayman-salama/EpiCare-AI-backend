# Firmware

`sketch_may19a.ino.hex` is a compiled Arduino UNO firmware image for the Proteus demo.

If you edit `sketch_may19a/sketch_may19a.ino`, rebuild it with Arduino IDE or Arduino CLI and update this hex file before the presentation.
